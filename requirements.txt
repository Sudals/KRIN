numpy>=1.26
pandas>=2.0
pyarrow>=14.0
scipy>=1.11
scikit-learn>=1.3
torch>=2.1
# Released-implementation adapters (models ending in _off) additionally need:
einops>=0.7
torch_geometric>=2.4
# torch_scatter has no universal wheel; install the build matching your torch/CUDA,
# e.g. pip install torch_scatter -f https://data.pyg.org/whl/torch-<version>+<cuda>.html
torch_scatter>=2.1
matplotlib>=3.8
PyYAML>=6.0
tables>=3.9
